generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Counter {
  key           String   @id
  value         Int      @default(0)
  updated_at    DateTime @default(now()) @updatedAt
  qstash_job_id String?

  @@map("counter")
}
